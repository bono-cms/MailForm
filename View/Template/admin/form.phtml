<?php

use Krystal\Form\Element;
use Krystal\Widget\GridView\GridViewWidget;
use Cms\Service\LanguageManager;
use Cms\View\Icon;
use MailForm\Collection\FieldTypeCollection;

defined('KRYSTAL') or die;

?>

<form class="card" data-group="form" autocomplete="off">
    <ul class="nav nav-tabs" role="tablist">
        <li role="presentation" class="nav-item">
            <a class="nav-link active" href="#page" role="tab" data-toggle="tab">
                <i class="fas fa-envelope-open-text"></i> <?php $this->show('Form'); ?>
            </a>
        </li>
        <li role="presentation" class="nav-item">
            <a class="nav-link" href="#message" role="tab" data-toggle="tab">
                <i class="fas fa-grip-horizontal"></i> <?php $this->show('Configuration'); ?>
            </a>
        </li>
        <?php if (!$new): ?>
        <li role="presentation" class="nav-item">
            <a class="nav-link" href="#fields" role="tab" data-toggle="tab">
                <i class="fas fa-clipboard-list"></i> <?php $this->show('Form fields'); ?> <small class="text-muted">(<?= count($extraFields); ?>)</small>
            </a>
        </li>
        <?php $this->loadPartialIfPossible('block-tab'); ?>
        <?php endif; ?>
    </ul>

    <div class="tab-content">
        <div class="tab-pane fade show active" id="page" role="tabpanel">
            <?php $this->loadPartial('language-tabs'); ?>

            <div class="tab-content">
                <?php foreach ($languages as $index => $language): ?>
                <?php $entity = LanguageManager::findByLangId($language->getId(), $form); ?>

                <div class="tab-pane fade <?= $index == 0 ? 'show active' : null; ?>" id="<?php printf('language-%s', $language->getId()); ?>" role="tabpanel">
                    <?= Element::hidden(sprintf('translation[%s][id]', $language->getId()), $entity->getId()); ?>
                    <?= Element::hidden(sprintf('translation[%s][web_page_id]', $language->getId()), $entity->getWebPageId()); ?>
                    <?= Element::hidden(sprintf('translation[%s][lang_id]', $language->getId()), $language->getId()); ?>

                    <?php $this->loadPartial('language-name', ['language' => $language]); ?>

                    <div class="form-group row">
                        <label class="col-lg-2 col-form-label"><?php $this->show('Name'); ?></label>
                        <div class="col-lg-10">
                            <div class="input-group">
                                <?= Element::text(sprintf('translation[%s][name]', $language->getId()), $entity->getName(), ['class' => 'form-control', 'placeholder' => $this->translate('Page name')]); ?>
                                <div class="input-group-append">
                                    <span class="input-group-text"><i class="bi bi-pencil"></i></span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="form-group row">
                        <label class="col-lg-2 col-form-label"><?php $this->show('Flash message'); ?></label>
                        <div class="col-lg-10">
                            <div class="input-group">
                                <?= Element::text(sprintf('translation[%s][flash]', $language->getId()), $entity->getFlash(), ['class' => 'form-control', 'placeholder' => $this->translate('Flash message after form submission. If empty, default is used')]); ?>
                                <div class="input-group-append">
                                    <span class="input-group-text"><i class="bi bi-pencil"></i></span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="form-group row">
                        <label class="col-lg-2 col-form-label"><?php $this->show('Description'); ?></label>
                        <div class="col-lg-10">
                            <?= Element::textarea(sprintf('translation[%s][description]', $language->getId()), $entity->getDescription(), ['data-wysiwyg' => 'true']); ?>
                        </div>
                    </div>

                    <div class="form-group row">
                        <label class="col-lg-2 col-form-label"><?php $this->show('Public offer before submission'); ?></label>
                        <div class="col-lg-10">
                            <?= Element::textarea(sprintf('translation[%s][terms]', $language->getId()), $entity->getTerms(), ['class' => 'form-control']); ?>
                        </div>
                    </div>

                    <?php if ($entity->isRegularForm()): ?>
                    <div class="options">
                        <div class="form-group row">
                            <label class="col-lg-2 col-form-label"><?php $this->show('Slug'); ?></label>
                            <div class="col-lg-10">
                                <div class="input-group">
                                    <?= Element::text(sprintf('translation[%s][slug]', $language->getId()), $entity->getSlug(), ['class' => 'form-control', 'placeholder' => $this->translate('URL slug for this page. By default its taken from the name')]); ?>
                                    <div class="input-group-append">
                                        <a href="#" class="input-group-text" data-slug-selector="<?= LanguageManager::generateSelector($language->getId()); ?>">
                                            <i class="bi bi-arrow-clockwise" data-toggle="tooltip" title="<?php $this->show('Refresh the slug'); ?>"></i>
                                        </a>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="form-group row">
                            <label class="col-lg-2 col-form-label"><?php $this->show('Change frequency'); ?></label>
                            <div class="col-lg-10">
                                <?= Element::select(sprintf('translation[%s][changefreq]', $language->getId()), $this->translateArray($changeFreqs), $entity->getChangeFreq(), ['class' => 'form-control']); ?>
                            </div>
                        </div>

                        <div class="form-group row">
                            <label class="col-lg-2 col-form-label"><?php $this->show('Priority'); ?></label>
                            <div class="col-lg-10">
                                <?= Element::select(sprintf('translation[%s][priority]', $language->getId()), $priorities, $entity->getPriority(), ['class' => 'form-control']); ?>
                            </div>
                        </div>

                        <div class="form-group row">
                            <label class="col-lg-2 col-form-label"><?php $this->show('Title'); ?></label>
                            <div class="col-lg-10">
                                <div class="input-group">
                                    <?= Element::text(sprintf('translation[%s][title]', $language->getId()), $entity->getTitle(), ['class' => 'form-control', 'data-input' => 'title', 'placeholder' => $this->translate('Page title')]); ?>
                                    <div class="input-group-append">
                                        <span class="input-group-text"><i class="bi bi-pencil"></i></span>
                                    </div>
                                </div>
                            </div>
                        </div>
                        
                        <div class="form-group row">
                            <label class="col-lg-2 col-form-label"><?php $this->show('Keywords'); ?></label>
                            <div class="col-lg-10">
                                <?= Element::textarea(sprintf('translation[%s][keywords]', $language->getId()), $entity->getKeywords(), ['class' => 'form-control', 'placeholder' => $this->translate('Keywords used for search engines')]); ?>
                            </div>
                        </div>

                        <div class="form-group row">
                            <label class="col-lg-2 col-form-label"><?php $this->show('Description'); ?></label>
                            <div class="col-lg-10">
                                <?= Element::textarea(sprintf('translation[%s][meta_description]', $language->getId()), $entity->getMetaDescription(), ['class' => 'form-control', 'placeholder' => $this->translate('Meta description for search engines')]); ?>
                            </div>
                        </div>
                    </div>
                    <?php endif; ?>

                </div>
                <?php endforeach; ?>
            </div>

            <?php $this->loadPartial('actions', [
                'new' => $new,
                'add' => sprintf('MailForm:Admin:Form@%s', $entity->isRegularForm() ? 'addAction' : 'addAjaxAction'),
                'edit' => 'MailForm:Admin:Form@editAction',
                'save' => 'MailForm:Admin:Form@saveAction',
                'cancel' => 'MailForm:Admin:Form@gridAction',
                'options' => $entity->isRegularForm()
            ]); ?>
        </div>

        <div class="tab-pane fade" id="message" role="tabpanel">
            <?= Element::hidden('form[id]', $entity->getId()); ?>
            <?= Element::hidden('form[type]', $entity->getType()); ?>

            <?php $this->loadPartialIfPossible('block-field'); ?>

            <div class="row">
                <div class="col-lg-12">
                    <div class="form-group row">
                        <label class="col-lg-2 col-form-label"><?php $this->show('Template'); ?></label>
                        <div class="col-lg-10">
                            <div class="input-group">
                                <?= Element::text('form[template]', $entity->getTemplate(), ['class' => 'form-control', 'placeholder' => $this->translate('Framework-compliant template file')]); ?>
                                <div class="input-group-append">
                                    <span class="input-group-text"><i class="bi bi-pencil"></i></span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="form-group row">
                        <label class="col-lg-2 col-form-label"><?php $this->show('Flash message position'); ?></label>
                        <div class="col-lg-10">
                            <?= Element::select('form[flash_position]', $this->translateArray($flashPositions), $entity->getFlashPosition(), ['class' => 'form-control']); ?>
                        </div>
                    </div>

                    <div class="form-group row">
                        <label class="col-lg-2 col-form-label"><?php $this->show('Enable CAPTCHA'); ?></label>
                        <div class="col-lg-10">
                            <div class="form-check">
                                <?= Element::checkbox('form[captcha]', $entity->getCaptcha(), ['class' => 'form-check-input']); ?>
                            </div>
                        </div>
                    </div>

                    <div class="form-group row">
                        <label class="col-lg-2 col-form-label"><?php $this->show('Enable auto complete on site'); ?></label>
                        <div class="col-lg-10">
                            <div class="form-check">
                                <?= Element::checkbox('form[autocomplete]', $entity->getAutocomplete(), ['class' => 'form-check-input']); ?>
                            </div>
                        </div>
                    </div>

                    <div class="form-group row">
                        <label class="col-lg-2 col-form-label"><?php $this->show('Enable SEO'); ?></label>
                        <div class="col-lg-10">
                            <div class="form-check">
                                <?= Element::checkbox('form[seo]', $entity->getSeo(), ['class' => 'form-check-input']); ?>
                            </div>
                        </div>
                    </div>

                    <div class="form-group row">
                        <label class="col-lg-2 col-form-label"><?php $this->show('Subject'); ?></label>
                        <div class="col-lg-10">
                            <div class="input-group">
                                <?= Element::text('form[subject]', $entity->getSubject(), ['class' => 'form-control', 'placeholder' => $this->translate('Message subject')]); ?>
                                <div class="input-group-append">
                                    <span class="input-group-text"><i class="bi bi-pencil"></i></span>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="form-group">
                        <?= Element::textarea('form[message]', $entity->getMessage(), ['id' => 'message-input', 'rows' => '13', 'class' => 'form-control', 'placeholder' => $this->translate('Target message')]); ?>

                        <?php if ($entity->getId()): ?>
                        <div class="mt-3">
                            <button data-button="generate" data-output="#message-input" data-url="<?= $this->url('MailForm:Admin:Form@messageAction', $entity->getId()); ?>" class="btn btn-primary">
                                <i class="bi bi-envelope"></i> <?php $this->show('Generate message'); ?>
                            </button>
                        </div>
                        <?php endif; ?>
                    </div>
                </div>

                <div class="col-lg-5">
                    <div class="alert alert-danger">
                        <?php if (!empty($subjectVars)): ?>
                        <p><?php $this->show('You can use the following subject variables'); ?>: </p>
                        <br />
                        <ul class="list-unstyled">
                            <?php foreach ($subjectVars as $subjectVar => $varName): ?>
                            <li>
                                <span class="badge badge-light border" style="cursor: pointer;" data-toggle="tooltip" data-placement="left" title="<?php $this->show('Click to copy'); ?>" data-button="clipboard" data-value="<?= $subjectVar; ?>">
                                    <?= $subjectVar; ?>
                                </span> 
                                <small>- <?= $varName; ?></small>
                            </li>
                            <?php endforeach; ?>
                        </ul>
                        <?php else: ?>
                        <p><?php $this->show('There must be at lease one text field created to make use of variables in the subject'); ?></p>
                        <?php endif; ?>
                    </div>
                </div>
            </div>
        </div>

        <div class="tab-pane fade" id="fields" role="tabpanel">
            <h3><i class="bi bi-list-task"></i> <?php $this->show('Form fields'); ?> <small>(<?= count($extraFields); ?>)</small></h3>

            <div class="my-3">
                <a class="btn btn-primary" href="<?= $this->url('MailForm:Admin:Field@addAction', $entity->getId()); ?>">
                    <?php $this->show('Add new field'); ?>
                </a>
            </div>

            <?= $this->widget(new GridViewWidget($extraFields, [
                'pk' => 'id',
                'tableClass' => 'table table-hover table-bordered table-striped',
                'columns' => [
                    [
                        'column' => 'id',
                        'label' => '#'
                    ],
                    [
                        'column' => 'name',
                        'attributes' => ['class' => null]
                    ],
                    [
                        'column' => 'type',
                        'translateable' => true,
                        'value' => function($row){
                            $typeCol = new FieldTypeCollection();
                            return $typeCol->findByKey($row['type']);
                        }
                    ],
                    [
                        'column' => 'order',
                    ],
                    [
                        'column' => 'required',
                        'label' => 'Required to be filled in',
                        'translateable' => true,
                        'value' => function($entity) {
                            return $entity->getRequired() ? 'Yes' : 'No';
                        }
                    ]
                ],
                'actions' => [
                    'edit' => function($row){
                        return Icon::edit(
                            $this->url('MailForm:Admin:Field@editAction', $row['id']),
                            $this->translate('Edit this field')
                        );
                    },
                    'remove' => function($row){
                        return Icon::remove(
                            $this->url('MailForm:Admin:Field@deleteAction', $row['id']),
                            $this->translate('Remove this field')
                        );
                    }
                ]
            ])); ?>
        </div>

        <?php $this->loadPartialIfPossible('block-content'); ?>
    </div>
</form>